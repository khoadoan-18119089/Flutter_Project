enum Video{
  video,
  short,
}
enum Volume {
  value,
  volumeOn,
  volumeOff,
}

enum Subtitle {
  on,
  off,
}

enum TypeText {
  value,
  titleLarge,
  titleMedium,
  titleSmall,
  bodyLarge,
  bodyMedium,
  bodySmall,
  labelLarge,
  labelMedium,
  labelSmall,
}

enum TypeBackBotton{
  arrowBack,
  close,
}

enum TypeButton{
  outlined,
  elevated,
  text,
  icon,
}

enum TuaVIdeo{
  forward,
  rewind,
}

enum NextBackVideo{
  next,
  back,
}

enum ButtonVideoIcon{
  backVideo,
  nextVideo,
  playPause,
}
